{
  "name": "nervult-nexus-static",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "echo 'Serve your HTML files using a simple HTTP server. Example: npx live-server . or python -m http.server'"
  }
}
